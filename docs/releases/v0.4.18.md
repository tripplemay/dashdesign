# DashDesign 0.4.18

- 图片模型可独立配置，整图海报、文生背景、图片编辑统一使用所选模型。
- 设置新增只读模型列表检查，不发起出图请求。
- 修复上游接口报错仍显示成功的问题；区分失败、部分成功、取消与仅准备请求包。
- 保留部分成功的图片与阶段记录，增加错误脱敏和图片有效性检查。
- 云端配置兼容旧客户端写入，避免遗漏字段覆盖图片模型。

部署顺序：先升级云端 API，再升级客户端。自定义模型需兼容当前 Images API 参数。

验证范围：本地 475 项测试通过；上游图片生成已单次实测恢复。完整海报、图片编辑及安装后的人工验收不等同于该单次探针结果。
