# Phase B cloud backend (server-only; NOT installed by the desktop client).
# The desktop client talks to the server over HTTP using `requests` (already in
# requirements.txt) and never imports anything from cloud/server.
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.31
SQLAlchemy>=2.0,<2.1
pydantic>=2,<3
python-multipart>=0.0.9,<0.1
# Required by fastapi.testclient (starlette TestClient) for the cloud test suite.
httpx>=0.27,<0.29
# Serverless adapter for Aliyun FC / Tencent SCF (API Gateway -> ASGI).
mangum>=0.17,<0.18
# Managed Postgres driver (prod). SQLite (stdlib) is used for local/dev/tests.
psycopg[binary]>=3.1,<4; platform_system != "Windows"
# Object storage (OSS) for uploaded documents is optional; the default document
# store is the local filesystem. Install the vendor SDK in the deploy image.
